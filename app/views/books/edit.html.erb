<h1>Edit Book</h1>

<%= form_with model: @book do |f| %>
  <p>Title</p>
  <%= f.text_field :title %>
  <p>Body</p>
  <%= f.text_area :body %><br>
  <%= f.submit %>
<% end %>

<%= link_to "Show", book_path(@book) %> | <%= link_to "Back", books_path %>
